{
  "jobs": [
    { "functionLocation": "/scheduled.js", "functionName": "nightlyCatalog",
      "description": "Catalog import kickoff", "executeConfig": { "cronExpression": "0 2 * * *" } },
    { "functionLocation": "/scheduled.js", "functionName": "hourlyCatalogContinue",
      "description": "Continue catalog batches", "executeConfig": { "cronExpression": "0 3-9 * * *" } },
    { "functionLocation": "/scheduled.js", "functionName": "hourlyStock",
      "description": "Stock refresh (1 call)", "executeConfig": { "cronExpression": "*/5 * * * *" } }
  ]
}
